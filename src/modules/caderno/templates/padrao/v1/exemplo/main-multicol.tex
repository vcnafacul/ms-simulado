% ===========================================================================
% PLANO B do card 01 — só existe para o smoke test.
%
% Se \documentclass[twocolumn]{exam} (o main.tex) não funcionar, este arquivo
% faz as duas colunas com o pacote multicol. Compile OS DOIS no mesmo upload
% e mantenha só o que funcionar melhor; o perdedor é apagado antes do PR.
%
% Só compila a partir do zip montado, onde todos os arquivos ficam no mesmo
% diretório — não de dentro de exemplo/.
% ===========================================================================
\documentclass[11pt,a4paper]{exam}

\input{preambulo}
\input{metadados}
\usepackage{multicol}

% draftwatermark carregado só no modo rascunho: carregá-lo sempre estampa
% "DRAFT" por padrão. Fica aqui, e não no preambulo.tex (onde diz que
% "pacotes moram aqui"), porque \ifcadernoRascunho só é ligado pelo
% metadados.tex, que é lido depois do preambulo — o \usepackage precisa
% vir depois de saber se o modo rascunho está ativo.
%
% \SetWatermarkText/\SetWatermarkScale/\SetWatermarkColor são a interface
% pré-3.0 do draftwatermark, hoje depreciada em favor de
% \DraftwatermarkOptions{...}. Mantida de propósito aqui — funciona nas
% duas versões — só pra um aviso de depreciação no Overleaf não virar
% mistério.
\ifcadernoRascunho
  \usepackage{draftwatermark}
  \SetWatermarkText{RASCUNHO}
  \SetWatermarkScale{1.2}
  \SetWatermarkColor[gray]{0.92}
\fi

\begin{document}

\capaCaderno

\begin{multicols}{2}
\begin{questions}
\input{conteudo}
\end{questions}
\end{multicols}

\end{document}
